<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <title>@ViewBag.Title</title>
        @Styles.Render("~/Content/PayRoll.css")
    </head>
    <body>
        <header>
            <div>
                <div class="tabbed_menu">
                    <p>@Html.ActionLink("Home", "RegisterNewEmployee", "Employee")</p>
                </div>
            </div>
        </header>
        <div id="body">
            @RenderSection("featured", false)
            <section>
                @RenderBody()
            </section>
        </div>
    </body>
</html>
